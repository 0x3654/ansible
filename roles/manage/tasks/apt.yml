---
- name: APT Management
  ansible.builtin.include_role:
    name: common
    tasks_from: apt.yml
  tags: [apt, upgrade]
  vars:
    common_required_packages: "{{ required_packages }}"
    common_install_docker_repo: "{{ install_docker_repo }}"
