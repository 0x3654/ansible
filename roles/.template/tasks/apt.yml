---
- name: APT Management
  ansible.builtin.include_role:
    name: common
    tasks_from: apt.yml
  tags: [apt, upgrade]
  vars:
    common_required_packages: "{{ {{ role_name }}_required_packages }}"
