---
# Required system packages
required_packages:  # noqa: var-naming[no-role-prefix]
  - docker-ce
  - docker-compose-plugin
  - certbot
  - nginx

# Docker repository installation control
install_docker_repo: true  # noqa: var-naming[no-role-prefix]
update_repo: true  # noqa: var-naming[no-role-prefix]
force_repo_update: false  # noqa: var-naming[no-role-prefix]

# User and paths (universal DRY variables for easy role copy)
folder_user: "{{ ansible_role_name | basename }}"  # noqa: var-naming[no-role-prefix]
pwd: "/server"  # noqa: var-naming[no-role-prefix]
path: "{{ pwd }}/{{ ansible_role_name | basename }}"  # noqa: var-naming[no-role-prefix]

# Variables for common role integration
files_source_dir: "{{ role_path }}/files/{{ ansible_role_name | basename }}"  # noqa: var-naming[no-role-prefix]
deploy_path: "/server/{{ ansible_role_name | basename }}"  # noqa: var-naming[no-role-prefix]

# File permissions
directory_mode: "0755"  # noqa: var-naming[no-role-prefix]
file_mode: "0644"  # noqa: var-naming[no-role-prefix]

# Jackett config
jackett_port: "9117"  # noqa: var-naming[no-role-prefix]
jackett_puid: "1000"  # noqa: var-naming[no-role-prefix]
jackett_pgid: "1000"  # noqa: var-naming[no-role-prefix]
jackett_timezone: "Europe/Moscow"  # noqa: var-naming[no-role-prefix]
jackett_cert_path: "{{ pwd }}/certbot"  # noqa: var-naming[no-role-prefix]
