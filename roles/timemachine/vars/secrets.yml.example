---
# Time Machine secrets (copy to vars/secrets.yml and fill in your values)

# SMB password for the timemachine user (openssl rand -hex 16)
# Set on first deploy only; to rotate: pdbedit -x timemachine on the host, rerun playbook
timemachine_smb_password: "change-me"
