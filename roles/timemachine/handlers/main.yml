---
# handlers file for timemachine

# reload-config не рвёт активные сессии (batman и пр.)
- name: Reload samba
  ansible.builtin.command: smbcontrol all reload-config
  changed_when: true
  listen: reload samba
